# MongoDB Atlas Setup

Set `MONGODB_URI` to an Atlas connection URI through local environment variables or a secret manager. Never commit credentials. Confirm the database name is not production before running bootstrap scripts in development.
