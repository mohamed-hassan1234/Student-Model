# Candidate Evaluation

Candidate adapters are evaluated against the same frozen evaluation set and compatible generation settings as the baseline. Phase 3 records scores, failure rate, latency and memory when available, and an explicit unavailable reason when evaluation cannot run.

Training loss alone is never sufficient for candidate selection.
