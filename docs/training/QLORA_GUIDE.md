# QLoRA Guide

QLoRA is optional. It requires compatible hardware, an approved quantization backend, and a `qlora` training configuration with explicit `quantization_mode`. Optional quantization packages are not mandatory for normal application install, tests, or CI.

If hardware or dependencies are unavailable, use smoke training or an approved remote training machine instead of pretending QLoRA is practical.
