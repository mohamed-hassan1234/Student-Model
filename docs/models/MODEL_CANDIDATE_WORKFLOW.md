# Model Candidate Workflow

Candidates are created only from completed training runs with a hashed adapter artifact, dataset version, training config, baseline evaluation, candidate evaluation, regression report, safety status, license status, rollback metadata, and human approval state.

Candidates are not production models. Registration does not deploy, promote, publish, merge, or change environment variables.
