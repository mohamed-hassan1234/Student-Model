# Citation Methodology

Every supported answer includes source IDs, document IDs, titles, URLs when available, section/page metadata, and relevance scores from retrieved chunks. The system must not invent source IDs, page numbers, sections, quotations, or scores.

Evidence statuses: `supported`, `partially_supported`, `insufficient_evidence`, `blocked_by_policy`, and `provider_unavailable`.
