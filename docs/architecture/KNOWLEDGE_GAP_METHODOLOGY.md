# Knowledge-Gap Methodology

Knowledge gaps are derived from multiple signals, including no approved sources, low approved-source count, low coverage score, missing verified questions, missing evaluation examples, and future human-reported errors.

Document volume is not treated as quality. A topic with many chunks can still have a gap if verification, licensing, citation quality, or evaluation coverage is weak.

Detected gaps are stored in `knowledge_gaps` with severity and recommended action.
