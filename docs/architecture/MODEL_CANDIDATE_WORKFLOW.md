# Model-Candidate Workflow

Model candidates record base model, revision, license, adapter location, adapter hash, training config, dataset version, evaluation set, metrics, safety results, regression results, creator, approval state, recommendation, and rollback metadata.

Registration does not deploy a model. Rollback metadata is required before a candidate can be registered.
