# Deployment Gate Policy

The Phase 2 gate produces a recommendation only.

A candidate may be recommended only when human approval exists, rollback metadata exists, dataset permissions are valid, citation correctness is not regressed, prompt-injection resistance is not regressed, safety checks pass, and licenses are compatible.

No deployment happens automatically.
