# Evaluation Methodology

Phase 2 creates a frozen evaluation-set foundation with categories for retrieval quality, citation correctness, factual grounding, technology topics, syntax, static quality, security awareness, unsupported-question handling, prompt-injection resistance, and source-conflict handling.

Evaluation records are separate from training records. They are never exported into training datasets.
