# MongoDB Bootstrap

Use `uv run python scripts/bootstrap_mongodb.py` to create or update Phase 0 system collections, validators, indexes, and schema-version records. The script is idempotent and reads connection settings from environment variables.
