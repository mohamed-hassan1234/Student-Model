# MongoDB Bootstrap

Use `uv run python scripts/bootstrap_mongodb.py` to create or update DevMind system collections, validators, indexes, and schema-version records through Phase 2. The script is idempotent and reads connection settings from environment variables.
