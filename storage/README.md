# Local Storage

This directory is for local runtime storage placeholders only. Uploaded documents, generated datasets, model files, and build artifacts must not be committed. Future file persistence should prefer MongoDB GridFS where durable file storage is required.
